name = input("Enter character : ")

length = len(name)

if length < 10:
    print("Character contain less than 10 character")

elif length >= 10:
    print("Character contain more than 10 character")
